@using Microsoft.AspNetCore.Identity;
@{
    ViewData["Title"] = "DeleteRole";
}

@*<h1>DeleteRole</h1>

<h3>Are you sure you want to delete this?</h3>*@
<div>

    @{
        IdentityRole role = ViewBag.Role;

        <h1> Delete @role.Name Role ?</h1>
        <hr />
        <p class="alert alert-danger">Note: Removing this role will remove any associated users automatically. </p>
        <h2> Are you sure you want to delete this role?</h2>
        @*<form asp-action="DeleteRoleConfirmed" type="post">
                <input type="hidden" name="roleId" value="@role.Id" />
                <div class="form-group">
                    <input type="submit" value="Delete" class="btn btn-danger" />
                </div>
            </form>
            <div>
                <a class="btn btn-primary" asp-action="Index">Cancel</a>
            </div>*@
        <form asp-action="DeleteRoleConfirmed">
            <input type="hidden" name="roleId" value="@role.Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a class="btn btn-primary" asp-action="Index">Cancel</a>
        </form>

    }
</div>
